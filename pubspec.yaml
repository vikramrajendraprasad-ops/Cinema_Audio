name: cinema_audio
description: Cinema Audio – External FFmpeg-based audio processor
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Audio file selection (Android SAF, no permissions needed)
  file_picker: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
