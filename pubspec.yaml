name: cinema_audio_clean
description: Cinema Audio Studio – AI assisted audio engine
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Safe, v2-embedding compatible
  file_picker: ^8.3.7

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
