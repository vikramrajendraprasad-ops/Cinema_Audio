name: cinema_audio
description: Cinema Audio – FFmpeg based audio processor
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # ⚠️ IMPORTANT: lock to SAFE version (CI + Android embedding compatible)
  file_picker: 5.5.0

flutter:
  uses-material-design: true
