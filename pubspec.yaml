name: cinema_audio
description: Cinema-grade audio enhancement engine
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  file_picker: ^8.3.7
  
  # ⚠️ UPDATED: Version 6.0.3 is required for Android 14 / Gradle 8+
  ffmpeg_kit_flutter_full_gpl: ^6.0.3

flutter:
  uses-material-design: true
