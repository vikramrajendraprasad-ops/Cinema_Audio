
name: cinema_audio
description: Cinema Audio – FFmpeg based audio processor
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Use a modern version (5.5.0 is too old and can break with v1 embedding removal)
  file_picker: ^8.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
