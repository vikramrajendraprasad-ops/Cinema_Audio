name: audio_cinema_studio
description: Cinema-grade offline audio processing using FFmpeg
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  file_picker: ^8.3.7
  ffmpeg_kit_flutter_full_gpl: 4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
